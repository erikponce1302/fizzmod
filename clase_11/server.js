const http = require("http")

const server = http.createServer((resp, req) =>{
    //cuando recibo la peticion de usuarios, realizo un get de los usuarios que se encuentran activos
    http.get()

    //cuando recibo una peticion de mensajes, realizo un get de los mensajes que se enviaron en el chat

    
})